# Daily market-price ledgers

New schema-v3 snapshot runs write `YYYY-MM-DD.json` here using the same official
daily-price response already fetched for model calculation. Coverage is the
current model Universe only. Files contain no scores or ranks and are never
silently overwritten.

The existing `2026-08-13` history snapshot predates this ledger and is not
backfilled or estimated.
