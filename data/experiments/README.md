# Model experiments

Each experiment is stored as an immutable `<experimentId>.json` file. Create
one before reading validation or holdout results. Do not edit hypothesis,
metrics, periods, or model version after creation; append result and decision
through a future audited evaluation command instead.
