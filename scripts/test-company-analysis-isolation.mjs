import assert from "node:assert/strict";import fs from "node:fs";const daily=fs.readFileSync(new URL("./create-daily-model-snapshot.mjs",import.meta.url),"utf8"),resolver=fs.readFileSync(new URL("./resolve-history-returns.mjs",import.meta.url),"utf8");assert.doesNotMatch(daily,/company-analysis|financial-statements/);assert.doesNotMatch(resolver,/analysis[\\/]company|financial-statements/);console.log("기업분석 실패 격리·resolver 불변 범위 테스트 통과");
