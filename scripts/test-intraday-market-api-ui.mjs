import assert from "node:assert/strict";import fs from "node:fs";const api=fs.readFileSync(new URL("../app/api/intraday-market-analysis/route.ts",import.meta.url),"utf8"),page=fs.readFileSync(new URL("../app/page.tsx",import.meta.url),"utf8"),panel=fs.readFileSync(new URL("../components/market-analysis/MarketAnalysisPanel.tsx",import.meta.url),"utf8"),top=fs.readFileSync(new URL("../app/api/top-stocks/route.ts",import.meta.url),"utf8"),resolver=fs.readFileSync(new URL("./resolve-history-returns.mjs",import.meta.url),"utf8");assert.match(api,/getKisQuote/);assert.doesNotMatch(api,/GetStockPriceInfo|api\/price/);assert.match(api,/marketSessionVerified:false/);assert.match(page,/api\/intraday-market-analysis/);assert.equal((page.match(/api\/intraday-market-analysis/g)??[]).length,1);assert.match(page,/realtimePrice\?\.marketStatus !== "open"/);assert.match(panel,/TOP·모델 순위·백테스트·최적화 사용 금지/);assert.doesNotMatch(top,/intraday/i);assert.doesNotMatch(resolver,/intraday/i);console.log("장중 API·UI·TOP/history 격리 테스트 통과");
