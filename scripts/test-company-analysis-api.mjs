import assert from "node:assert/strict"; import fs from "node:fs"; const source=fs.readFileSync(new URL("../app/api/company-analysis/route.ts",import.meta.url),"utf8");
assert.match(source,/data["'], ["']analysis["'], ["']company/);assert.match(source,/INVALID_STOCK_CODE/);assert.match(source,/INVALID_DATE/);assert.match(source,/\^\[0-9A-Z\]\{6\}\$/);assert.match(source,/COMPANY_ANALYSIS_NOT_FOUND/);assert.doesNotMatch(source,/fetch\s*\(/);assert.doesNotMatch(source,/api\/financial|DART_API|KIS_APP|calculateCompanyAnalysis/);assert.match(source,/Cache-Control["']: ["']no-store/);console.log("company analysis API 저장 조회 전용 계약 테스트 통과");
