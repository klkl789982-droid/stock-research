import assert from "node:assert/strict"; import {createCompanyAnalysisSnapshot,validateCompanyAnalysisSnapshot} from "../lib/company-analysis-snapshot.mjs";
const input={requestedDate:"2026-08-17",generatedAt:"x",formulaHash:"a",sourceManifest:{},dataQuality:{},universeSummary:{count:2},records:[{code:"005930"},{code:"000660"}]}; const one=createCompanyAnalysisSnapshot(input),two=createCompanyAnalysisSnapshot(input); assert.deepEqual(one,two);assert.deepEqual(one.records.map((r)=>r.code),["000660","005930"]);assert.deepEqual(validateCompanyAnalysisSnapshot(one),[]);console.log("company snapshot serializer·hash 테스트 통과");
