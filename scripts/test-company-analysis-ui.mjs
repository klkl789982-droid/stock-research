import assert from "node:assert/strict";import fs from "node:fs";const page=fs.readFileSync(new URL("../app/page.tsx",import.meta.url),"utf8"),panel=fs.readFileSync(new URL("../components/company-analysis/CompanyAnalysisPanel.tsx",import.meta.url),"utf8");
assert.match(page,/\/api\/company-analysis\?code=/);assert.doesNotMatch(page,/\/api\/financial\?code=/);assert.doesNotMatch(page,/const roe\s*=|const profitabilityScore|const totalScore/);assert.match(page,/<CompanyAnalysisPanel/);assert.equal((page.match(/api\/company-analysis/g)??[]).length,1,"탭 클릭은 API를 호출하지 않아야 합니다.");assert.match(page,/searchControllerRef\.current\?\.abort/);assert.match(page,/requestId !== searchRequestIdRef\.current/);assert.match(page,/companyResponse \? await companyResponse\.json\(\) : null/);assert.match(panel,/DART를 호출하거나 브라우저 계산·KIS 가격 fallback을 사용하지 않습니다/);assert.doesNotMatch(panel,/fetch\s*\(|calculateCompanyAnalysis/);console.log("company analysis UI 조회 전용·stale guard 테스트 통과");
